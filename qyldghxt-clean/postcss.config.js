/**
 * PostCSS 配置文件
 * 
 * 插件配置：
 * - tailwindcss: 用于处理Tailwind CSS的功能
 * - autoprefixer: 自动添加浏览器前缀
 * 
 * 作用：
 * - 处理CSS后处理任务
 * - 确保跨浏览器兼容性
 * - 优化CSS代码
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
